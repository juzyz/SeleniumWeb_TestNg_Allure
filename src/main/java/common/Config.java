package common;

public class Config {

    /* Set platform amd browser used for automation */
    public  static final String PLATFORM_AND_BROWSER = "win_firefox";
    /*
    Clear browser cookies after each iteration
    if true - clear cookies
     */
    public  static final Boolean CLEAR_COOKERS_AND_STORAGE = true;

    /*
    To keep the browser open after suite
    if true - browser close
     */
    public static final Boolean HOLD_BROWSER_OPEN = true;
}
